<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button aria-expanded="false" class="navbar-toggle collapsed" data-target="#bv-navbar" data-toggle="collapse" type="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "BaristaVision", root_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="bv-navbar">
      <ul class="nav navbar-nav">
        <li class="<%= ('active' if controller_name == "tablets") %>">
          <%= link_to "Tablets", tablets_path %>
        </li>
        <li class="<%= ('active' if controller_name == "orders") %>">
          <%= link_to "Orders", root_path %>
        </li>
      </ul>
      <div class="navbar-right">
        <ul class="nav navbar-nav">
          <% if user_signed_in? %>
            <div class="navbar-text">
              <em>
                <%= current_user.email %>
              </em>
            </div>
            <li>
              <%= link_to destroy_user_session_path, method: :delete, class: 'navbar-link' do %>
                <i class="fa fa-sign-out"></i>
                Sign out
              <% end %>
            </li>
          <% else %>
            <li>
              <%= link_to new_user_session_path, class: 'navbar-link' do %>
                <i class="fa fa-sign-in"></i>
                Sign in
              <% end %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>
